{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build && composer install --optimize-autoloader --no-dev && php artisan key:generate && php artisan config:cache",
        "start": "php artisan serve --host=0.0.0.0 --port=8080"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
